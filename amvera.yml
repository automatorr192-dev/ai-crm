meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

run:
  # База заявок обязана пережить передеплой — значит только /data и абсолютным путём.
  persistenceMount: /data
  containerPort: 80
  cmd: python -m uvicorn app:app --host 0.0.0.0 --port 80
